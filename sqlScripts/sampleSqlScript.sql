
-- to run this in mySQL in terminal:
-- $ source cs311/sampleSqlScript.txt
-- can put the file anywhere. can be .txt or .sql

use movies;
select * from movies limit 10;
use textbook;
select title, dept_name from course limit 10;